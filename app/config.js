const config = {
  API_URL: "http://62.4.16.132:3000",
  GAME_FOLDER: "LoMGame",
  LOCAL_UPDATE_FILE: "updates.json"
};

config.LOCAL_UPDATE_FILE_PATH = `${config.GAME_FOLDER}/${config.LOCAL_UPDATE_FILE}`;

export default config;
